module Sweep
  module Matcher
    class PatternMatcher
      def is_match?(file, patternlist)
        # for each regex in pattern list, 
        # does the file match the pattern?

      end
    end
  end
end
